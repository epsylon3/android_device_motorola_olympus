#
# ADDITIONAL_DEFAULT_PROPERTIES (From bootmenu/config)
#
# This is required when property service is not ready, on early init
#

ro.product.device=olympus
ro.secure=0
ro.allow.mock.location=0
ro.debuggable=1
persist.service.adb.enable=1

# Forbid format of these partitions in mount menu (use only rm -r)
ro.cwm.forbid_format=/misc,/devtree,/config,/boot,/recovery,/pds,/logo

